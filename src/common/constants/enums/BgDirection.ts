// eslint-disable-next-line no-shadow -- disabled
export enum BgDirection {
    // eslint-disable-next-line no-unused-vars -- disabled
    LEFT = 0,
    // eslint-disable-next-line no-unused-vars -- disabled
    RIGHT = 1,
    // eslint-disable-next-line no-unused-vars -- disabled
    RANDOM = 2,
}
